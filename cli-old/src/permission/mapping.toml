[permissions.camera]
ios = [
    { info_plist_key = "NSCameraUsageDescription", default_description = "This app needs camera access for capturing photos and videos.", entitlement = "" }
]
android = [
    { manifest_permission = "android.permission.CAMERA", dangerous = true, min_sdk = 0, max_sdk = 0 }
]

[permissions.microphone]
ios = [
    { info_plist_key = "NSMicrophoneUsageDescription", default_description = "This app needs microphone access for audio recording.", entitlement = "" }
]
android = [
    { manifest_permission = "android.permission.RECORD_AUDIO", dangerous = true, min_sdk = 0, max_sdk = 0 }
]

# ===================================================================
# LOCATION
# ===================================================================

[permissions.location]
ios = [
    { info_plist_key = "NSLocationWhenInUseUsageDescription", default_description = "This app needs location access while in use.", entitlement = "" },
    { info_plist_key = "NSLocationAlwaysAndWhenInUseUsageDescription", default_description = "This app needs location access.", entitlement = "" }
]
android = [
    { manifest_permission = "android.permission.ACCESS_FINE_LOCATION", dangerous = true, min_sdk = 0, max_sdk = 0 },
    { manifest_permission = "android.permission.ACCESS_COARSE_LOCATION", dangerous = true, min_sdk = 0, max_sdk = 0 }
]

[permissions.location-always]
ios = [
    { info_plist_key = "NSLocationAlwaysUsageDescription", default_description = "This app needs location access even when not in use.", entitlement = "" },
    { info_plist_key = "NSLocationAlwaysAndWhenInUseUsageDescription", default_description = "This app needs location access.", entitlement = "" }
]
android = [
    { manifest_permission = "android.permission.ACCESS_BACKGROUND_LOCATION", dangerous = true, min_sdk = 29, max_sdk = 0 }
]

[permissions.location-when-in-use]
ios = [
    { info_plist_key = "NSLocationWhenInUseUsageDescription", default_description = "This app needs location access while in use.", entitlement = "" }
]
android = [
    { manifest_permission = "android.permission.ACCESS_FINE_LOCATION", dangerous = true, min_sdk = 0, max_sdk = 0 }
]

# ===================================================================
# PHOTOS / MEDIA
# ===================================================================

[permissions.photos]
ios = [
    { info_plist_key = "NSPhotoLibraryUsageDescription", default_description = "This app needs access to your photo library.", entitlement = "" }
]
android = [
    { manifest_permission = "android.permission.READ_MEDIA_IMAGES", dangerous = true, min_sdk = 33, max_sdk = 0 },
    { manifest_permission = "android.permission.READ_MEDIA_VIDEO", dangerous = true, min_sdk = 33, max_sdk = 0 },
    { manifest_permission = "android.permission.READ_EXTERNAL_STORAGE", dangerous = true, min_sdk = 0, max_sdk = 32 }
]

[permissions.photos-add-only]
ios = [
    { info_plist_key = "NSPhotoLibraryAddUsageDescription", default_description = "This app needs permission to save photos to your library.", entitlement = "" }
]
android = [
    { manifest_permission = "android.permission.WRITE_EXTERNAL_STORAGE", dangerous = true, min_sdk = 0, max_sdk = 28 }
]

[permissions.media-library]
ios = [
    { info_plist_key = "NSAppleMusicUsageDescription", default_description = "This app needs access to your media library.", entitlement = "" }
]
android = [
    { manifest_permission = "android.permission.READ_MEDIA_AUDIO", dangerous = true, min_sdk = 33, max_sdk = 0 }
]

# ===================================================================
# CONTACTS / CALENDAR / REMINDERS
# ===================================================================

[permissions.contacts]
ios = [
    { info_plist_key = "NSContactsUsageDescription", default_description = "This app needs access to your contacts.", entitlement = "" }
]
android = [
    { manifest_permission = "android.permission.READ_CONTACTS", dangerous = true, min_sdk = 0, max_sdk = 0 },
    { manifest_permission = "android.permission.WRITE_CONTACTS", dangerous = true, min_sdk = 0, max_sdk = 0 }
]

[permissions.calendar]
ios = [
    { info_plist_key = "NSCalendarsUsageDescription", default_description = "This app needs access to your calendar.", entitlement = "" }
]
android = [
    { manifest_permission = "android.permission.READ_CALENDAR", dangerous = true, min_sdk = 0, max_sdk = 0 },
    { manifest_permission = "android.permission.WRITE_CALENDAR", dangerous = true, min_sdk = 0, max_sdk = 0 }
]

[permissions.reminders]
ios = [
    { info_plist_key = "NSRemindersUsageDescription", default_description = "This app needs access to your reminders.", entitlement = "" }
]
android = [] # Android uses calendar permissions

# ===================================================================
# NOTIFICATIONS / NETWORK
# ===================================================================

[permissions.notifications]
ios = [] # handled by UNUserNotificationCenter
android = [
    { manifest_permission = "android.permission.POST_NOTIFICATIONS", dangerous = true, min_sdk = 33, max_sdk = 0 }
]

[permissions.internet]
ios = []
android = [
    { manifest_permission = "android.permission.INTERNET", dangerous = false, min_sdk = 0, max_sdk = 0 }
]

[permissions.bluetooth]
ios = [
    { info_plist_key = "NSBluetoothAlwaysUsageDescription", default_description = "This app uses Bluetooth to connect to nearby devices.", entitlement = "" }
]
android = [
    { manifest_permission = "android.permission.BLUETOOTH_CONNECT", dangerous = true, min_sdk = 31, max_sdk = 0 },
    { manifest_permission = "android.permission.BLUETOOTH_SCAN", dangerous = true, min_sdk = 31, max_sdk = 0 },
    { manifest_permission = "android.permission.BLUETOOTH", dangerous = false, min_sdk = 0, max_sdk = 30 },
    { manifest_permission = "android.permission.BLUETOOTH_ADMIN", dangerous = false, min_sdk = 0, max_sdk = 30 }
]

[permissions.bluetooth-peripheral]
ios = [
    { info_plist_key = "NSBluetoothPeripheralUsageDescription", default_description = "This app uses Bluetooth to communicate with peripherals.", entitlement = "" }
]
android = [
    { manifest_permission = "android.permission.BLUETOOTH_ADVERTISE", dangerous = true, min_sdk = 31, max_sdk = 0 }
]

[permissions.nfc]
ios = [
    { info_plist_key = "NFCReaderUsageDescription", default_description = "This app uses NFC to read tags.", entitlement = "com.apple.developer.nfc.readersession.formats" }
]
android = [
    { manifest_permission = "android.permission.NFC", dangerous = false, min_sdk = 0, max_sdk = 0 }
]

[permissions.wifi-state]
ios = []
android = [
    { manifest_permission = "android.permission.ACCESS_WIFI_STATE", dangerous = false, min_sdk = 0, max_sdk = 0 },
    { manifest_permission = "android.permission.CHANGE_WIFI_STATE", dangerous = false, min_sdk = 0, max_sdk = 0 }
]

# ===================================================================
# BIOMETRICS / SENSORS / HEALTH
# ===================================================================

[permissions.biometrics]
ios = [
    { info_plist_key = "NSFaceIDUsageDescription", default_description = "This app uses Face ID for secure authentication.", entitlement = "" }
]
android = [
    { manifest_permission = "android.permission.USE_BIOMETRIC", dangerous = false, min_sdk = 28, max_sdk = 0 }
]

[permissions.motion]
ios = [
    { info_plist_key = "NSMotionUsageDescription", default_description = "This app uses motion sensors for activity tracking.", entitlement = "" }
]
android = [
    { manifest_permission = "android.permission.ACTIVITY_RECOGNITION", dangerous = true, min_sdk = 29, max_sdk = 0 }
]

[permissions.health-read]
ios = [
    { info_plist_key = "NSHealthShareUsageDescription", default_description = "This app reads health data to provide personalized insights.", entitlement = "com.apple.developer.healthkit" }
]
android = [
    { manifest_permission = "android.permission.BODY_SENSORS", dangerous = true, min_sdk = 20, max_sdk = 0 }
]

[permissions.health-write]
ios = [
    { info_plist_key = "NSHealthUpdateUsageDescription", default_description = "This app writes health data to track your progress.", entitlement = "com.apple.developer.healthkit" }
]
android = [
    { manifest_permission = "android.permission.BODY_SENSORS", dangerous = true, min_sdk = 20, max_sdk = 0 }
]

# ===================================================================
# APPLE-SPECIFIC
# ===================================================================

[permissions.speech-recognition]
ios = [
    { info_plist_key = "NSSpeechRecognitionUsageDescription", default_description = "This app uses speech recognition for voice commands.", entitlement = "" }
]
android = [] # uses RECORD_AUDIO

[permissions.siri]
ios = [
    { info_plist_key = "NSSiriUsageDescription", default_description = "This app integrates with Siri for voice control.", entitlement = "com.apple.developer.siri" }
]
android = []

[permissions.homekit]
ios = [
    { info_plist_key = "NSHomeKitUsageDescription", default_description = "This app controls your HomeKit accessories.", entitlement = "com.apple.developer.homekit" }
]
android = []

[permissions.apple-music]
ios = [
    { info_plist_key = "NSAppleMusicUsageDescription", default_description = "This app needs access to Apple Music.", entitlement = "" }
]
android = []

# ===================================================================
# ANDROID SPECIFIC
# ===================================================================

[permissions.read-external-storage]
ios = []
android = [
    { manifest_permission = "android.permission.READ_EXTERNAL_STORAGE", dangerous = true, min_sdk = 0, max_sdk = 32 }
]

[permissions.write-external-storage]
ios = []
android = [
    { manifest_permission = "android.permission.WRITE_EXTERNAL_STORAGE", dangerous = true, min_sdk = 0, max_sdk = 28 }
]

[permissions.vibrate]
ios = []
android = [
    { manifest_permission = "android.permission.VIBRATE", dangerous = false, min_sdk = 0, max_sdk = 0 }
]

[permissions.wake-lock]
ios = []
android = [
    { manifest_permission = "android.permission.WAKE_LOCK", dangerous = false, min_sdk = 0, max_sdk = 0 }
]

[permissions.receive-boot-completed]
ios = []
android = [
    { manifest_permission = "android.permission.RECEIVE_BOOT_COMPLETED", dangerous = false, min_sdk = 0, max_sdk = 0 }
]

[permissions.read-phone-state]
ios = []
android = [
    { manifest_permission = "android.permission.READ_PHONE_STATE", dangerous = true, min_sdk = 0, max_sdk = 0 }
]

[permissions.call-phone]
ios = []
android = [
    { manifest_permission = "android.permission.CALL_PHONE", dangerous = true, min_sdk = 0, max_sdk = 0 }
]

[permissions.send-sms]
ios = []
android = [
    { manifest_permission = "android.permission.SEND_SMS", dangerous = true, min_sdk = 0, max_sdk = 0 }
]

[permissions.receive-sms]
ios = []
android = [
    { manifest_permission = "android.permission.RECEIVE_SMS", dangerous = true, min_sdk = 0, max_sdk = 0 }
]

[permissions.read-sms]
ios = []
android = [
    { manifest_permission = "android.permission.READ_SMS", dangerous = true, min_sdk = 0, max_sdk = 0 }
]